return {
  "folke/trouble.nvim",
  opts = {
    modes = {
      symbols = {
        win = {
          -- Values < 1 are a percentage of the screen.
          -- Values > 1 are static column width.
          size = 40,
        },
      },
    },
  },
}
